APP_NAME=AcademIQ Backend
APP_ENV=development
API_HOST=127.0.0.1
API_PORT=8000

JWT_SECRET_KEY=change-this-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60

# Comma-separated origins. Use * only for local testing.
CORS_ALLOW_ORIGINS=*

# ── MySQL Database ──────────────────────────────────────────────
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=smart_class
# Use smart_admin for full access, smart_student for read-only student access
DB_USER=smart_admin
DB_PASSWORD=Admin@SmartClass2025!
